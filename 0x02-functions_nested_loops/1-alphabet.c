#include "main.h"
/**
 * print alphabet prints alphabet in small letters
 * return (0)
 */

void print_alphabet(void);
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	{
	_putchar('\n');
}
